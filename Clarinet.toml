[project]
name = "uma-rental-algorithm"
authors = ["Uma Protocol Team"]
description = "Decentralized rental protocol on Stacks blockchain"
telemetry = false
requirements = []

[contracts.uma-rental-core]
path = "contracts/uma-rental-core.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = true
trusted_sender = false
trusted_caller = false
callee_filter = true
